apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sms-spam-rnn-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sms-spam-rnn-svc
                port:
                  number: 80
